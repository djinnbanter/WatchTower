/**
 * Legacy Updates tab — pack-impact UI now lives on the Library filter + project page.
 * Keep VersionDelta / ModUpdateImpactSection exports for shared consumers.
 */
export { VersionDelta, ModUpdateImpactSection } from './updates-impact';
