# Legacy Preact dashboard (archive)

This tree is the **former** production dashboard (Preact). It is kept for reference and historical diffs.

**Shipping UI:** [`web/dashboard-alpha`](../dashboard-alpha) (React + Vite). NeoForge `syncDashboard` builds and embeds that tree only.

Do not point Gradle sync back at this folder without an explicit rollback plan.
