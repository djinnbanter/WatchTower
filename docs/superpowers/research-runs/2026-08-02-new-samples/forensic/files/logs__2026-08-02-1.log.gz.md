# Forensic file note — logs/2026-08-02-1.log.gz

**rel:** `logs/2026-08-02-1.log.gz`  
**kind:** rotate_gz  
**line_count:** 26887  
**read_complete:** true  

## Time span
- First useful timestamp: `02Aug2026 00:00:11.714` (mid-runtime; file opens on KubeJS tick ERROR — no boot/`Done` in this rotate)
- Last useful timestamp: `02Aug2026 15:31:08.636` (WatchTower Modrinth scan complete during shutdown tail)

## Session phases
- Boot: **Not present** in this rotate. Session continues overnight from a prior boot after the 23:06 bounce (boot lives in an earlier/unlisted rotate or overnight log not in this batch). Opens already ticking.
- Runtime: Long day session 00:00–15:31. 255 joins / 276 leaves. **No hard crashes, no watchdog FATAL, no NoSuchMethodError, no Sable body-removed.** Chronic lag: **943×** `Can't keep up` (first L240 00:14:20, last L26712 15:30:50). Mid-session resource reloads (~02:48 and ~13:49) re-emit createfood/KubeJS/loot floods and OPAC-BetterCommands “Registered commands after server start” (3×). Destroy-block mismatch WARNs dominate (hundreds–thousands). Horse/boat move WARNs. LuckPerms MariaDB connection validate WARNs recurring. Recipe book “unrecognized recipe … removed now” for Create encased_chain_drive variants. Shtreimel sub-level add/remove activity continues without serialize crash this day. WatchTower Modrinth scan lines present.
- Stop / crash / restart: Clean ops stop — `Stopping the server` L26843 / 15:31:00 → `Stopping server` L26863. Worlds save successfully; shtreimel sub-levels unloaded with reason=UNLOADED just before stop. No crash report.

## Notable events
- Day-after-incident: OPAC and Sable remain installed but do not re-crash; lag is the main hurt.
- KubeJS `vots.js#119` `redeclaration of var voteData` fires repeatedly across the day (~31 stack clusters) — tick script bug, noisy ERROR flood, not fatal.
- Resource reload windows reproduce FB-08/FB-10 class spam without a reboot.
- Leaves > joins (276 vs 255) — churn / overnight attrition.
- Final line WatchTower Modrinth scan complete — WT alive through shutdown path.

## Player / ops impact
- Hurt vs quiet: Playable with **persistent overload** (nearly 1k cant-keep-up). No crash kicks. Ops afternoon stop at 15:31.

## Noise vs hurt
- Dominant spam (full traversal):
  - `Can't keep up!`: **943×** (L240–L26712) — primary hurt signal
  - destroy-block mismatch: ~678+ / ~579+ / ~412+ distinct pos clusters — lag/desync noise
  - vehicle move WARNs — lag symptom
  - createfood ~3873 / kubejs recipe ~4224 / loot_parse ~1680 — reload+datapack noise (FB-08/10)
  - KubeJS vots.js stacks — script ERROR spam
  - LuckPerms hikari MariaDB validates — DB flakiness noise (related FB-11 class)
- Real incidents: chronic tick lag only. No new crash-class FB hits.

## Surprises / script-blind candidates
- Mid-rotate without `Done (` — scanners assuming boot markers per file will miss session continuity.
- OPAC still registered on reloads after Aug 1 crashes — latent risk if party chat reused.
- Sable sublevels unload cleanly on stop this day — contrasts FB-04 save-path failure.
- Recipe-book “removed now” errors cluster after reloads — player recipe book pollution.

## WT relevance / Prior pass
- Related: lag aftermath of Aug 1; latent **FB-01/FB-02** (OPAC still present); **FB-04** not reproduced; FB-08/10/11 noise. No FB-03/FB-05 this day.
- Ingestion: seen
