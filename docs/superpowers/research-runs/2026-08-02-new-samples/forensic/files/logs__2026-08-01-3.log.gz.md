# Forensic file note — logs/2026-08-01-3.log.gz

**rel:** `logs/2026-08-01-3.log.gz`  
**kind:** rotate_gz  
**line_count:** 2173  
**read_complete:** true  

## Time span
- First useful timestamp: 01Aug2026 18:59:35.986
- Last useful timestamp: 01Aug2026 18:59:48.911

## Session phases
- Boot: Starts ~6s after `-2` cut. Same pack (NeoForge 21.1.248 / 1.21.1). Gets through mod discovery, DISTXFORM, MariaDB addon fail, Jade plugin load, then into loot-table worker parse flood.
- Runtime: **Never reaches `Done (`.** No players, no tick loop.
- Stop / crash / restart: Process killed / rotated mid-boot while Worker-Main-3 is still emitting loot-parse ERRORs; final line is `emi_loot: parsing loot tables` @ L2173. Immediately succeeded by full boot in `2026-08-01-4` at 18:59:54.

## Notable events
- Entire file is an **aborted boot** (~13 seconds wall clock).
- Still loads `opac_better_commands-1.5.jar` (L113) — same mismatch stack that will kill `-4`.
- ~560 loot-parse ERRORs dominate almost the whole second half of the file.
- No crash report written from this rotate (no FATAL / Preparing crash report).

## Player / ops impact
- Hurt vs quiet: Brief downtime only (seconds). Players from `-2` reconnect onto `-4`. Panel/host bounce cost, not a mod crash.

## Noise vs hurt
- Dominant spam: loot-parse ERROR flood (~560) + DISTXFORM + MariaDB addon fail — same boot spam as every other Aug 1 rotate.
- Real incidents: Host/panel abort of an in-progress boot between busy afternoon session (`-2`) and the FB-01 evening session (`-4`).

## Surprises / script-blind candidates
- Census correctly shows no `server_done` / no `tick_lag`. Easy for a skim to treat as “empty day”; it is a **restart artifact** on the incident timeline.
- Abrupt end mid-loot-parse with no JVM crash dump — suggests SIGTERM/kill from panel, not Minecraft exception.

## WT relevance / Prior pass
- Related: timeline glue between afternoon play (`-2`) and **FB-01** session (`-4`). OPAC Better Commands still present.
- Ingestion: seen
