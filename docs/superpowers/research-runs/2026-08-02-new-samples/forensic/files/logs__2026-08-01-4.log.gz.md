# Forensic file note — logs/2026-08-01-4.log.gz

**rel:** `logs/2026-08-01-4.log.gz`  
**kind:** rotate_gz  
**line_count:** 8030  
**read_complete:** true  

## Time span
- First useful timestamp: 01Aug2026 18:59:54.034
- Last useful timestamp: 01Aug2026 19:25:10.801

## Session phases
- Boot: Immediate retry after aborted `-3`. `Done (3.870s)` @ L3701 (19:00:14). Same pack; `opac_better_commands` 1.5 registers commands after server start (L2175).
- Runtime: ~25 minutes of heavy multiplayer. Many joins (73 join lines), server-full rejects, OPAC party chat/invites, chronic `Can't keep up` lag storm, SDLink webhook 429s, Watchtower lag auto-captures.
- Stop / crash / restart: **Hard crash @ 19:24:51** — `NoSuchMethodError` on OPAC API via party chat **command**, crash saved to `crash-2026-08-01_19.24.51-server.txt`, then `Stopping server` and mass “Server closed” disconnects. Spark async sampler then warns `RejectedExecutionException: Server already shutting down` while rotating profiler jobs (shutdown hygiene, not root cause).

## Notable events
- **FB-01 root event (log-side):**
  - L7792–7795: `Encountered an unexpected exception` → `java.lang.NoSuchMethodError: 'xaero.pac.common.server.player.config.IPlayerConfigManager xaero.pac.common.server.IServerData.getPlayerConfigs()'`
  - Stack: `opac_better_commands@1.5` `PartyMessenger.sendPartyMessage` ← `PartyChatCommand.lambda$register$3` ← brigadier chat command path (`performUnsignedChatCommand`).
  - L7819–7820: FATAL Preparing crash report UUID `90c31b13-…`; saved `crash-2026-08-01_19.24.51-server.txt`.
- Immediate social context: OPAC party chat traffic seconds earlier (`Edwardpia's Party`, `Maksymi19's Party` invite/join @ L7784–7788); lag still ~3s behind @ L7791 just before the exception.
- Lag storm across session: **67×** `Can't keep up`, max **14423 ms / 288 ticks** @ L6426 (19:11:45); 36 events ≥5s, 7 ≥10s.
- Server-full: login reject “The server is full!” @ L7764–7765 while lagging.
- Spark shutdown WARN @ L7985–8024 (`Server already shutting down` on async world-info gather) — related class to **FB-06** spark shutdown hygiene, but here secondary to OPAC crash (not the Jul 31 profiler-inactive crash).
- Discord chat reacts mid-shutdown: `[Discord]kiccsavo1: DAMN IT` / `Thyste: LOL` @ L7914–7915.

## Player / ops impact
- Hurt vs quiet: **Full server wipe for everyone online** — hard crash from a party-chat command. Preceded by minutes of heavy lag (felt as stutter/rubber-banding) and full-server join denials. This is the first hard Aug 1 incident in the rotate set; later FB-02/03 (20:42/20:43) and FB-04/05 (21:49/21:50) continue the evening.

## Noise vs hurt
- Dominant spam (boot): loot-parse ~560, createfood/KubeJS recipe WARNs ~1285 (**FB-08**), DISTXFORM, MariaDB addon fail.
- Runtime noise: SDLink webhook 429 (~109), vehicle “moved too quickly”, destroy-block pos mismatch, POI mismatch.
- Real incidents: **OPAC NSM party-chat command crash (FB-01)**; chronic tick lag; Spark shutdown WARN after crash.

## Surprises / script-blind candidates
- Census already flags `nosuchmethod:1` + `server_stop:1` — good. Stack entry via **command** (not listener) must stay distinct from **FB-02** listener path at 20:42.
- Spark `RejectedExecutionException` on shutdown is easy to mis-primary if crash classifier stares at the tail of the log instead of the NSM.
- OPAC party messages still succeed via OPAC’s own logger moments before Better Commands command path explodes — mismatch is addon-specific.

## WT relevance / Prior pass
- Related gap-matrix ids / FB-*: **FB-01** (primary). Precursor to **FB-02/FB-03** same evening. Spark shutdown WARN echoes **FB-06** class. Recipe flood **FB-08**.
- Ingestion: seen
