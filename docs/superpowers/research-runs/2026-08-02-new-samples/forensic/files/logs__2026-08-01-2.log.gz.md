# Forensic file note — logs/2026-08-01-2.log.gz

**rel:** `logs/2026-08-01-2.log.gz`  
**kind:** rotate_gz  
**line_count:** 4982  
**read_complete:** true  

## Time span
- First useful timestamp: 01Aug2026 18:54:37.590
- Last useful timestamp: 01Aug2026 18:59:29.753

## Session phases
- Boot: Full NeoForge 21.1.248 / MC 1.21.1 dedicated boot on Linux host `/home/container` (Velocity-fronted). Mod scan includes `opac_better_commands-1.5.jar`, `openpartiesandclaims`, Sable, Spark, Watchtower 1.1.2, createfood, Jade. Standard DISTXFORM client-class noise + GriefLogger MariaDB addon fail + ~560 loot-parse ERRORs + ~1285 createfood/KubeJS recipe WARNs during data load.
- Runtime: `Done (4.026s)` @ L3702 (18:54:57). Players flood in immediately (IronBlackSamurai first join L3733). Busy early-session play: advancements, LPC chat, OPAC party ticket updates, PixelsAntiCheat mod-hash checks, voicechat auth. Owner chat notes a donation (“thank you qasa, the server will benefit”).
- Stop / crash / restart: **No clean stop and no crash in this rotate.** File ends mid-session at 18:59:29 on a normal leave (`seamadiga`). Next rotate is aborted boot `2026-08-01-3` ~6s later → panel/host restart bounce before the FB-01 session in `-4`.

## Notable events
- Chronic early lag: 12× `Can't keep up` (max **7132 ms / 141 ticks** @ L4944 18:58:52; also 5875 ms @ L4972).
- Watchtower auto-captured lag incident @ L4933 (`2026-08-01T16-58-36Z`, MSPT 50.3, TPS 19.9) — confirms WT live and reacting before evening crashes.
- SDLink Discord webhook **HTTP 429** storm: ~50 `WebhookClient` ERRORs ~18:55–18:56 (rate-limit noise, not gameplay crash).
- PixelsAntiCheat: at least one kick path sample earlier (`KickOnSuspiciousContent`); later joins show hash match OK.
- `opac_better_commands` registered at boot and party/forceload traffic is healthy — **no `NoSuchMethodError` yet** (precursor only; FB-01 fires in `-4` ~25 min later).
- Sable auth on joins (“Beginning attempted authentication”) — no body-removed / sublevel crash here.

## Player / ops impact
- Hurt vs quiet: Mild–moderate lag and one short webhook flood; server stays up. Hurt is playability (stutters), not a wipe. Session cut by external restart into `-3`/`-4`.

## Noise vs hurt
- Dominant spam patterns (first/last + approx volume after full traversal):
  - Loot-table missing deps (~560 ERRORs, boot window ~L1613+): dndecor / railways / create_connected unknown registry keys — noise.
  - createfood + KubeJS recipe parse flood (~1400 WARNs in boot) — **FB-08** noise_drown class.
  - DISTXFORM / mixin client-on-server (~30) — noise.
  - GriefLogger MariaDB addon fail (2) — soft fail, addon disabled.
  - SDLink 429 webhook (~50) — integration noise.
- Real incidents: Tick-lag burst near end of rotate; Watchtower lag auto-capture; imminent restart chain leading to FB-01.

## Surprises / script-blind candidates
- Rotate ends without `Stopping server` — census `server_stop:0` is correct; timeline must treat as **hard cut / host restart**, not graceful stop.
- Discord 429 volume is high but not in primary crash patterns — easy to miss vs recipe flood.
- Owner/player chat about donation + “sent death threats” (joke/context) — social noise only.

## WT relevance / Prior pass
- Related gap-matrix ids / FB-*: **FB-01 precursor** (OPAC Better Commands loaded + active parties; crash not yet). **FB-08** recipe flood. Watchtower auto-capture proves ingestion path for lag peeks works on this host.
- Ingestion: seen
