# Forensic file note — logs/2026-07-30-2.log.gz

**rel:** `logs/2026-07-30-2.log.gz`  
**kind:** rotate_gz  
**line_count:** 1824  
**read_complete:** true  

## Time span
- First useful timestamp: `30Jul2026 00:00:00.104` (mid-runtime rotate; no ModLauncher boot in this file)
- Last useful timestamp: `30Jul2026 06:32:04.640` (periodic Sable sub-level saves; rotate cut, no Stopping server)

## Session phases
- Boot: None — overnight continuation of prior Jul 29 session.
- Runtime: Almost entirely Sable/shtreimel + aeroclaims ship thrash on plot `(10000,10002)` and neighbors. Players IronBlackSamurai / tuxmango active ~00:03–00:32 (teleports, entity kills, creative/spectator). After ~00:32 server mostly idle aside from LuckPerms Hikari WARNs, Sable 5-min saves, BlueMap reload ~00:35, failed login probes (rootcheck123, DinoGlitcher).
- Stop / crash / restart: No crash, no Stopping server — log4j rotate at 06:32 into morning reboot (`2026-07-30-3`).

## Notable events
- L1–~1408: Continuous `shtreimel` sub-level add/remove (~every 1–2 s) paired with `aeroclaims` “New unregistered ship detected” — same plot centers around `(-48, ~70–84, 348)`.
- L301 / L833: IronBlackSamurai “moved too quickly” with large Y delta (~40) — aeronautics/ship motion symptom.
- L354–366, later clusters through L1821: LuckPerms Hikari `Failed to validate connection … Connection.setNetworkTimeout cannot be called on a closed connection` (81×).
- L1409–1437: ERROR `Failed to handle packet ServerboundUseItemOnPacket` → `UnsupportedOperationException: Cannot change blocks in nonexistent plot holder` via Sable `ServerChunkCache` mixin — player block-place on bad ship/plot state; suppressed, non-fatal.
- L1482–1484: Ops toggles whitelist (“There are no whitelisted players” / already on) while tuxmango online.
- L1496–1500: BlueMap reload; L1500s ERROR listener exception onEnable (one-shot).
- Spark viewer socket close L1489 (idle client).

## Player / ops impact
- Hurt vs quiet: Mild. Ship thrash is log noise; the nonexistent-plot place ERROR is a real Sable/plot glitch for the player but not an outage. Overnight after 00:32 is quiet.

## Noise vs hurt
- Dominant spam patterns (first/last + approx volume after full traversal):
  - `shtreimel.lifecycle` add/remove: **900×** (L1–L1408) — dominant file content
  - `aeroclaims` unregistered ship: **458×** (L3–L1407) — paired with shtreimel
  - LuckPerms Hikari closed-connection: **81×** (L354–L1821) — DB pool churn overnight
  - `moved too quickly`: 13× — ship/teleport motion
  - Sable “Saving sub-levels”: 237× — periodic quiet-period heartbeat
- Real incidents: Sable nonexistent plot holder packet fail (L1409); BlueMap listener ERROR; whitelist ops chatter.

## Surprises / script-blind candidates
- shtreimel/aeroclaims **INFO** thrash is census-blind if patterns only watch WARN/ERROR — can drown real signals in volume and MSPT cost without looking “broken.”
- Closed-connection LuckPerms WARNs overnight while idle — pool/maxLifetime config smell, not gameplay.
- `Cannot change blocks in nonexistent plot holder` is a sharp Sable+Create Aeronautics interaction fingerprint.

## WT relevance / Prior pass
- Related gap-matrix ids / FB-*: New candidate vs current matrix (no shtreimel/aeroclaims row yet). Adjacent to later `crash-0801-sable` world (same pack). LuckPerms/DB noise adjacent to `signal-db-addon` class.
- Ingestion: seen
