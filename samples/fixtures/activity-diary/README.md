# Activity diary fixtures

Unit tests for `PackChangeActivityScanner` build temp `mods/` + `config/` trees (see `PackChangeActivityScannerTest`). No checked-in jar binaries are required.

Preview UI samples live in `web/dashboard/data/ops-cache.json` → `activity.events`.
